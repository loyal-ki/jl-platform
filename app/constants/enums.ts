export enum RegistrationWizardStepType {
    Information = 0,
    Occupation = 1,
    CurrentLevel = 2,
    Confirmation = 3,
}

export enum RegistrationStatusType {
    Completed = 0,
    InProgress = 1,
    Pending = 2,
}
